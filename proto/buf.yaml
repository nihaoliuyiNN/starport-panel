version: v2
modules:
  - path: .
lint:
  use:
    - STANDARD
  except:
    # Connect 是双向流，帧类型按方向命名（AgentFrame / ServerFrame）比 ConnectRequest/Response 直观
    - RPC_REQUEST_STANDARD_NAME
    - RPC_RESPONSE_STANDARD_NAME
breaking:
  use:
    - FILE
